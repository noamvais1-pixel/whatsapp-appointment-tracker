# מעקב פגישות מוואטסאפ

התוכנה מתחברת לחשבון וואטסאפ (של כל טלפון, לא רק של המחשב הזה), קוראת את הצ'אטים,
ומנהלת רשימה של **פגישות, שיחות טלפון ומעקבים** שהיא מוצאת בהם.
יש לוח בקרה בכתובת http://localhost:3123, התראות במק כשנמצא משהו חדש,
ואפשרות לקבל סדר יום יומי לוואטסאפ של עצמך.

## איך מפעילים

**לחיצה כפולה על "מעקב פגישות" בשולחן העבודה.** זה מפעיל את התוכנה ברקע
ופותח את לוח הבקרה בחלון משלו (לא בדפדפן). אם התוכנה כבר רצה, זה רק פותח את החלון.
אפשר לגרור את האייקון ל-Dock.

**סגירת החלון לא עוצרת כלום** - התוכנה ממשיכה לקרוא הודעות ברקע והחיבור לוואטסאפ
נשאר כמו שהוא. פותחים את החלון שוב מתי שרוצים.

כדי לעצור באמת, לוחצים **יציאה** בלוח הבקרה. בפתיחה הבאה התוכנה מתחברת מחדש לוואטסאפ
(בדרך כלל 10-60 שניות, בלי סריקה חדשה).

אופציונלי: לחיצה כפולה על `Start at login (optional).command` בתוך תיקיית הפרויקט
גורמת לתוכנה לעלות לבד בכל כניסה למק (לחיצה נוספת מבטלת).

## חיבור טלפון

בפעם הראשונה מופיע קוד QR בלוח הבקרה. בטלפון שרוצים לעקוב אחריו:
**וואטסאפ ← הגדרות (או שלוש הנקודות) ← מכשירים מקושרים ← קישור מכשיר**, וסורקים.
החיבור נשמר בתיקיית `data`, אז סורקים רק פעם אחת.

**החלפת טלפון:** לוחצים **החלפת טלפון** בלוח הבקרה. המכשיר המקושר מוסר מהטלפון הנוכחי,
התוכנה שואלת אם לשמור או למחוק את הפריטים שהגיעו ממנו, ומופיע קוד QR חדש לטלפון הבא.

## לוח הבקרה

בראש הלוח יש שני מצבים: **משימות** (הפגישות והמעקבים) ו**צ'אטים** - רשימת כל
הצ'אטים כמו בוואטסאפ ווב, עם חיפוש, ההודעה האחרונה ומונה לא-נקראו. לחיצה על צ'אט
פותחת את השיחה לצידו, ואפשר לכתוב ולשלוח משם.

- לשוניות **היום / קרוב / באיחור / מעקבים / בלי תאריך / כל הפתוחים / בוצע**.
- לכל פריט יש כפתורי **בוצע**, **ביטול** ו**תאריך**.
- כל פריט מציג את ההודעה המדויקת שממנה הוא נוצר, כדי שאפשר יהיה לבדוק.
- **לחיצה על כותרת הפריט** פותחת את הצ'אט בצד: רואים את ההודעות האחרונות מהחשבון
  המקושר, ההודעה שממנה נוצר הפריט מסומנת, ואפשר לכתוב תשובה שנשלחת דרך
  הוואטסאפ המקושר (לא דרך הוואטסאפ של המחשב).
- **הודעות קוליות, תמונות, סרטונים וקבצים** מופיעים בתוך השיחה כתצוגה מטושטשת עם
  כפתור **הורדה**. שום דבר לא יורד לבד - רק מה שלוחצים עליו. אחרי ההורדה: הודעה קולית
  עם נגן, תמונה (לחיצה פותחת בגודל מלא ב-Preview), סרטון עם נגן, וקובץ עם כפתור שפותח
  אותו בתוכנה המתאימה במק. מה שהורד נשמר בתיקיית `data/media`. קבצים מעל 40MB לא יורדים.
  קובץ ישן שוואטסאפ כבר לא מחזיק יציג "וואטסאפ לא סיפק את הקובץ" עם אפשרות לנסות שוב.
- אפשר להוסיף פריטים ידנית בשורה שלמעלה.
- **לבדוק הודעות חדשות עכשיו** קורא מחדש את הצ'אטים האחרונים מוואטסאפ ומנתח מיד את מה שממתין.
- **סדר היום** מציג את אותו טקסט שנשלח בסיכום היומי.

אם צ'אט דוחה או מבטל משהו אחר כך, הפריט מתעדכן במקום להיווצר כפול.
כשמישהו כותב "שלחתי" / "בוצע", המעקב נסגר.

## מה קורה עם פריטים שהמועד שלהם עבר

פגישה או שיחה שהמועד שלה עבר לפני יותר מ-12 שעות (`AUTO_CLOSE_PAST_HOURS`):
- מועד **שאושר** נסגר כ"בוצע" ועובר ללשונית "בוצע / בוטל".
- מועד **שרק הוצע ולא אושר** (למשל "אני יכולה הערב, ואת?" בלי תשובה) לא נסגר: הוא הופך
  למעקב "לקבל תשובה מ-X על …" עם תאריך של היום, ונשאר ברשימה עד שמטפלים בו.
מעקבים אף פעם לא נסגרים לבד.

## הודעות שלא נענו

אם כתבת אחרונה בצ'אט ואף אחד לא ענה במשך 24 שעות (`NO_REPLY_HOURS`), התוכנה קוראת את
השיחה האחרונה ומחליטה אם באמת צריך מעקב: שאלה פתוחה, הצעת מחיר או פגישה שלא אושרו,
החזר כספי שמחכים לו, או משהו שהבטחת לחזור אליהם בקשר אליו. סיומים של "תודה / נתראה"
ובוטים מדולגים. התזכורת מציגה את הסיבה, והיא נסגרת לבד כשהצד השני עונה או כשכותבים
לו מהחלונית.

## הגדרות (קובץ `.env`)

| הגדרה | מה היא עושה |
|---|---|
| `GEMINI_API_KEY` | מפתח ה-API של Gemini (https://aistudio.google.com/apikey). חובה. |
| `GEMINI_MODEL` | איזה מודל קורא את הצ'אטים. ברירת המחדל `gemini-3.5-flash-lite` היא היחידה שמפתח חינמי יכול להשתמש בה כל היום (על `gemini-3.5-flash` מפתח חינמי מקבל רק כ-20 בקשות ביום). עם חיוב כדאי לעבור ל-`gemini-3.5-flash`. |
| `GEMINI_MIN_INTERVAL_MS` | מרווח בין בקשות ל-Gemini. `13000` מתאים למפתח חינמי; עם חיוב אפשר לרדת ל-`1000`. |
| `TIMEZONE` | כדי ש"מחר ב-3" ינחת על היום הנכון. ריק = אזור הזמן של המק. |
| `BACKFILL_DAYS` | כמה ימים אחורה לקרוא בחיבור הראשון. |
| `IGNORE_GROUPS` | `true` מדלג על קבוצות (מומלץ). |
| `ONLY_CHATS` / `SKIP_CHATS` | שמות או מספרים (מופרדים בפסיק) לכלול / להוציא. |
| `AUTO_CLOSE_PAST_HOURS` | אחרי כמה שעות מהמועד לסגור פגישה/שיחה שעברה. `0` = אף פעם. |
| `NO_REPLY_HOURS` | אחרי כמה שעות בלי תשובה לבדוק אם צריך מעקב. `0` מכבה. |
| `DAILY_DIGEST_TIME` | למשל `08:00` כדי לקבל את סדר היום לוואטסאפ של עצמך. ריק = כבוי. |

## איך יודעים שזה עובד

הפס בראש החלון הוא התשובה. הוא ירוק ("הכל עובד") רק כשהתוכנה הוכיחה את זה ממש עכשיו:
בכל דקה היא שואלת את וואטסאפ אם החיבור חי, וכל הודעה שנקלטת וכל ניתוח שהצליח נרשמים.
לחיצה על הפס מציגה את הראיות: מתי הייתה בדיקת החיבור האחרונה, מתי נקלטה ההודעה האחרונה,
מתי הניתוח האחרון הצליח וכמה הודעות ממתינות.

- **ירוק** - הכל עובד. אם כתוב "X הודעות בניתוח", הניתוח בעיצומו ויסתיים לבד.
- **צהוב** - וואטסאפ עובד, אבל משהו משני תקוע (למשל Gemini). התוכנה ממשיכה לנסות.
- **אדום** - לא עובד, וכתוב למה. אם נדרש משהו ממך (למשל סריקת QR), זה כתוב שם.

כשהמצב משתנה מעובד ללא-עובד (או חוזר לעבוד), מגיעה גם התראה במק. אחרי שינה של המחשב
התוכנה בודקת מיד את החיבור ומתחברת מחדש בעצמה.

## אם משהו מפסיק לעבוד

התוכנה בודקת את עצמה. אם וואטסאפ משנה משהו והיא כבר לא מצליחה לקרוא הודעות, מופיע
פס אדום בראש לוח הבקרה והתראה במק. אותו דבר אם החיבור נותק או אם יש בעיה עם מפתח
ה-Gemini (פס צהוב). התוכנה לא יכולה לתקן לבד שינוי של וואטסאפ - הספרייה שהיא משתמשת בה
נכתבת על ידי מתנדבים, והתיקון מגיע ימים או שבועות אחרי השינוי. כשמופיע הפס האדום,
צריך לבקש עדכון של התוכנה.

## שינה של המחשב

כשהמק ישן, התוכנה ישנה איתו ולא קוראת הודעות. לכן, כל עוד התוכנה רצה, היא מונעת מהמק
להירדם מחוסר פעילות (המסך עדיין יכול להיכבות). סגירת המכסה עדיין מרדימה את המחשב.
כדי לכבות את זה: `KEEP_AWAKE=false` בקובץ `.env`. אחרי התעוררות, התוכנה מתחברת מחדש
לוואטסאפ לבד.

## חשוב לדעת

- **פרטיות:** טקסט ההודעות מהצ'אטים המותרים נשלח ל-Gemini של גוגל לצורך הניתוח.
  כל השאר נשאר במק הזה בתיקיית `data`. אפשר להגביל עם `ONLY_CHATS`.
- **מפתח Gemini חינמי מוגבל בקצב.** התוכנה מרווחת את הבקשות, ולכן הקריאה הראשונה של
  חשבון עמוס לוקחת זמן (כמה הודעות בדקה). הכותרת בלוח מציגה כמה הודעות עוד ממתינות.
  הפעלת חיוב על המפתח והורדת `GEMINI_MIN_INTERVAL_MS` מאיצות מאוד.
- **חיבור לא רשמי:** התוכנה משתמשת באותו מנגנון "מכשיר מקושר" של וואטסאפ ווב דרך
  ספרייה לא רשמית. היא רק קוראת, ושולחת רק מה שכותבים בחלונית או את הסיכום היומי.
- הודעות קוליות אפשר להשמיע בחלון השיחה, אבל הן לא מתומללות, ולכן הניתוח לא יודע מה נאמר בהן.
- כדי להתחיל מאפס לגמרי, מוחקים את תיקיית `data`.
